1. 클래스내에 메소드 
 -. 11개의 메소드만으로 이루어진 object클래스는 최상위 클래스로 모든클래스는 object클래스를 상속받는다. 
 -. object클래스의 tostring(): 객체의 문자정보를 리턴
 -. 객체의 문자 정보란? 객체를 문자열로 표현한 값을 말한다.
 -. 기본적으로 "패키지명.클래스명@16진수해시코드"로 구성된 문자정보를 준다. 
 
 2. 메서드 오버라이딩(재정의) or 오버로딩
 -. 295p~ 300p
 
 3. Class!!
  [접근제한자][제어자] class 클래스명{
  -. field → 데이터를 저장
    > [접근제한자][제어자] 타입 필드명[=초기값];
  -. method → 동작,기능
    > [접근제한자][제어자] 리턴유형 메서드명 (){}
    }
    
  4. 자동차 클래스에 만든 타이어 클래스 상속하기.
  
  5. 데이터출력 (필드) > 전역변수.
   -. 참조변수명.필드명; : 필드의 값을 가져오기 → get
    -. 참조변수명.필드명=값; : 필드의 값을 설정하기 → set
    
  6. method명 선언및 출력하기.
    -. (객체 선언시 사용된 변수).메소드명();
    → method를 선언할때마다. 속도를 계속 늘리기. 계속 줄이기.
    → main클래스에서 필드선언과 메소드 선언하기.
    
   -. method 선언은 
      객체생성변수 메소드명 (매개변수값);
      사용 되는데 사용된 클래스 method를 에서 실행문과 return문이 있다. 실행문은 매개변수값을 받고 실행되고 return은 사용된 main method에서 그 값을 표현해준다. (실행문에 syso가 있다면 출력은 무조건 된다. )(return에서 반영된 값은 main method에서 사용될때 그 값이 나온다.)
   
